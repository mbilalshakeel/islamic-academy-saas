export default function HomePage() {
  return (
    <main className="min-h-screen flex items-center justify-center relative overflow-hidden" style={{ background: "var(--surface-0)" }}>
      <div className="text-center relative" style={{ padding: "var(--sp-6)" }}>
        <h1 className="ds-h1">ICI Multi-Tenant Platform</h1>
        <p style={{ color: "var(--text-tertiary)", marginTop: "var(--sp-2)", fontSize: "var(--fs-body)" }}>
          Scaffolding phase — see /login
        </p>
      </div>
    </main>
  );
}
